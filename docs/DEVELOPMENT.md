# 式札かさね 開発記録

『月蝕綺譚 -Luna Occulta-』二次創作の第2弾。2026-08-20 に着手し、08-22 に GitHub Pages で公開、
08-24 に OGP・PV・わいわいタウン掲載までの公開一式を完了した。

第1弾[御霊おとし](https://github.com/hiroHGxx/mitama-otoshi)の資産（札絵・琴サンプル・トンマナ・ビルド構成）を
流用しているため、**本書は「2作目でしか出てこなかった話」を中心に記録する**。
1作目で確立した型は [mitama-otoshi/docs/DEVELOPMENT.md](https://github.com/hiroHGxx/mitama-otoshi/blob/main/docs/DEVELOPMENT.md) を参照。

実装仕様は [SPEC.md](SPEC.md)、ボイス制作は [VOICE.md](VOICE.md) が正本。

## 1. 1作目から引き継いだもの・変えたもの

| | 引き継ぎ | 備考 |
|---|---|---|
| 札絵 | ◎ そのまま | 公式素材蔵ランク10 |
| 琴の効果音 | ◎ そのまま | Lovart生成曲から切り出したワンショット |
| トンマナ（CSS変数） | ◎ そのまま | 公式MCP `get_design_tone` 由来 |
| 単一HTMLビルド | ◎ 構成を踏襲 | `scripts/build-dist.js` |
| iOS対策 | ◎ そのまま | ダブルタップズーム抑止・`gesturestart` |
| 顔アイコン | ✕ 作り直し | 札絵の切り抜き → **公式配布のちびアイコン**へ（08-24） |
| 描画 | ✕ 新規 | 落ち物（Matter.js物理）→ **等角投影2.5Dの自前描画** |
| BGM | ✕ 差し替え | メインテーマ → キャラソング「Freehand」（ネムのテーマ） |
| ボイス | ＋ 新規 | **12場面。本作最大の新規要素** |

**素材の流用が効くのが二次創作シリーズの強み。**1作目で素材調達・トンマナ適用・ビルドを
片付けてあったので、2作目はゲーム性とボイスに時間を使えた。

## 2. ボイス導入（本作の主題）

12場面の台詞を入れた。段が新しい御霊に替わったときに、その御霊が**名乗る**。

- 出どころは2系統: **Irodori-TTS**（無料ローカル・公式配布wavを参照音声に）と **ElevenLabs**（公式指定Voice ID）
- **公式指定Voice IDが現行ElevenLabsから削除されていた**（ネム=Aria／ネコマタ=Charlotte）→ 公式配布ボイスから代役を立てた
- 「頁」の読みが崩壊する（イーラーショー／ユーポー）等の読み間違いに時間を取られた。**最終的な解は「カタカナで書く」「そもそもその語を選ばない」**

工程で得たものは [VOICE.md](VOICE.md) に**「次に作るときの手順書」**として全面的に書き直してある。
6手順（台詞設計 → 声選び → 生成 → 読み上げ検証 → 後処理 → 実機で通し聴き）＋音量の設計＋つまずき集。
**以後ボイスを作るときはまずこれを読む。**

### 音量とダッキング

ボイスを載せたあと、琴と木魚が台詞とほぼ同じ大きさで鳴っていて名乗りの頭が埋もれていた。
`src/game.js` のコメント「BGMは控えめに、効果音を主役にする」はボイス追加前の判断で、
**ボイスを足したときにSE側を見直していなかった**という取りこぼし。

SEを一律に下げず、**台詞を `voiceBus` に分離して鳴っている間だけ `sfxBus` を約-7dB沈める**方式にした。
琴は浄化の唯一の報酬フィードバックで、8回の台詞のために痩せさせるのは割に合わないため。
数値と実測は [VOICE.md「音量の設計」](VOICE.md#音量の設計)。

## 3. 自動検証パイプライン（`scripts/playtest.js`）

80段までの通し確認を人力でやるのは無理なので、自動化した。
サーバー起動 → 3ルート検証（満月成就／夜明け／稽古が記録を残さないこと）→ スクショまで面倒を見る。

```bash
npm i --no-save puppeteer-core   # 初回のみ
node scripts/playtest.js         # → /tmp/kasane-*.png・全✅で exit 0
```

ここで分かった重要な制約:

- **Chrome の `--virtual-time-budget` では rAF 駆動のゲームループが進まない。**
  「一瞬で長時間ぶんを進める」目論見は外れ、0段のまま撮れてしまう
- 正解は **puppeteer-core で実時間待ち**。`page.waitForFunction()` で終了条件を待ち、
  `page.evaluate()` でDOMの値を吸い出してから screenshot
- **タブがバックグラウンドでも rAF は止まる**ので、実ブラウザのタブで放置検証もできない
- `#autotest` は「ぴったりの位置に来たら置く」ので80段到達まで実時間で約79秒かかる。
  夜明け側は `#autocut`（わざと40pxずらす）で数段で終わらせる

**デバッグ用の自動プレイhashを最初から仕込んであったのが効いた。**

## 4. 08-23 の最終チェック

全コードを読み直し、3ルートの自動検証を通して6件の粗を修正した。**公開後に自分で見つけた**もので、
どれもプレー中に気づける類ではない。

1. 72段の演出が二重発火していた
2. **稽古モード（裏モード）がBEST・称号を汚染していた** — 稽古の到達を本番の誉れに混ぜない
3. Pages初回訪問で開幕の栞の台詞が鳴らない — 音素材のfetchが間に合わない → **読み込み時から先読み**に変更
4. ミュートで台詞が止まらない
5. SPECが存在しないスクリプトを参照していた → `scripts/playtest.js` を新設して実体を合わせた
6. 陳腐化したコメント

**通しで自動検証できる仕組みを作ってから読み返すと、粗がまとめて出てくる。**

## 5. 踏んだところ

- **Artifact の CSP は `fetch("data:")` をブロックする。**単一HTML化した音源data URIを `fetch()` で読むと
  Artifact 上で全滅する。`atob()` で直接 ArrayBuffer 化 → `decodeAudioData` が正解。
  **合成音のフォールバックがあると「鳴っているのに実は読めていない」事故が静かに起きる**
  （1作目のArtifact版も琴が合成音のままだったと、これで気づいた）
- **稽古モードのロゴ3タップは `click` ではなく `pointerdown` で数える。**
  iOSのダブルタップズーム抑止（touchendの `preventDefault`）が2回目以降の click を打ち消すため
- **公式ちびアイコンへの差し替え**（08-24）: `get_spirit` の sheets に顔アイコン（透過・1024²・栞含む全キャラ）が
  追加されていた。札絵の切り抜きより軽く綺麗で、`faces.js` が 544KB→198KB になった。
  **公式素材は増えることがあるので、更新時に台帳を見直す**
- **80段の演出から咲耶のカットインを撤去**（08-24）。「余韻1.3秒 → 栞の満願のページ → 栞の声」に整理した。
  Artifact版が 13.1MB → 10.9MB に軽くなった副次効果も大きい

## 6. 公開まわり

### GitHub 公開（gh CLI なし）

`gh` を入れずに、Keychain の認証情報＋GitHub API だけでリポジトリ作成 → push → Pages 有効化 →
反映確認（200になるまでポーリング → ローカルビルドと `diff` 一致）まで到達した。手順は
手元の `kitan-works/docs/RELEASE.md`（非公開）に記録した。

このとき**公開リポジトリの author を noreply に統一**する必要があり、
1作目（御霊おとしの全30コミット）も遡って書き換えた。

### 公開一式（08-24）

| 成果物 | 内容 |
|---|---|
| OGP | `ogp.png`（1200×600・栞＋御霊の階梯）＋metaタグ。ソースは `scripts/ogp-card.html`。Twitterbot UA で配信確認 |
| PV | `scripts/pv/kasane-pv.mp4`（縦720×1280・29秒・BGM＋ボイス3本）。**クリア画面は見せず**72段の栞「最後のページは、わたくしが」で引きを作った |
| わいわいタウン | サムネ `waiwai-thumb-A.png`（塔＋満月・文字なし）／プレビュー動画 `waiwai-preview.mp4`（640²・14秒・無音・冒頭からプレー画面） |
| X告知 | PV添付・リンクはリプ欄 |

- **PVとサムネの撮影は専用ハッシュで行った**。`#autowobble`（本番ルールのまま中くらいに細らせる自動プレイ）と
  `nofloat`（UI文字を消す）を本体に足してある。**「文字が消える隙間」をフレーム探しするより、消すフラグを足すほうが早い**
- **わいわいタウンの登録はブラウザ自動操作でほぼ完結した**（ログインのみ手動）。`/submit` はプレーンHTMLフォーム
- **PagesのURLはXでカードが表示されなかった**。OGP反映前にクロールされたURLはキャッシュが残る。
  わいわいタウン側のカードは出ていたのでそちらで誘導した

## 7. 次に作るときへ

- **80段クリアという「終わりのある一夜」は当たりだった。**到達点があると称号・演出・台詞の置き場が決まり、PVの締めも作れる
- **ボイスを入れると1日では終わらない。**声選びと読みの修正が工数の大半を占める
- 御霊おとしへ移植したいもの: ①公式ちびアイコンへの顔差し替え ②CSP修正（Artifactプレビューで琴が合成音フォールバックのまま）
- シリーズ共通のノウハウは手元の `kitan-works/docs/`（非公開）にまとめてある。3作目はそこから読む

---

## 8. 【2026-08-30】御霊おとしの更新を移す（第1便）— 文字コード・ズームの出口・セーブ保全

御霊おとしが08-28〜29の健診と更新で得たものを、こちらへ順に移す。下調べは
`kitan-circle/data/articles/20260830-1200-kasane-port-diff.json`（14項目）。
本便は「1行で済む事故予防」「実害のある5行」「セーブ保全」の3つ。

### `dist/artifact.html` に文字コードの宣言が無かった（未爆の地雷）

- **症状**: 出ていなかった。ブラウザの推測がたまたま当たっていただけ
- **原因**: `scripts/build-dist.js` の `inlineScripts` のあと、head から
  `<meta[^>]*>` をまとめて剥がしている。**charset ごと落ちる**。非ASCIIを含む
  `<title>式札かさね</title>` が先頭に来る構造と噛み合うと、ブラウザが文字コードを推測する
- **向こうで起きたこと**: 御霊おとしは同じ作りで **IBM866（キリル文字）と誤推測**され、
  08-29に全文字化け＋横はみ出しを起こした。**`file://` でも起きる**。
  **こわいのは、中身が変わると推測結果も変わること。**長らく偶然うまくいっていて、
  ボイスを足してファイルが変わった日に転んだ
- **直し方**: 書き出しの先頭に `<meta charset="utf-8">` を必ず戻す（非ASCIIの `<title>` より前）
- **確かめ方**: `file://` で `document.characterSet` が `UTF-8`・`<h1>` と `.game-title` が
  読める文字・`scrollWidth === clientWidth`（横はみ出しなし）・`pageerror` 0

### `gesturestart` を無条件に塞いでいた（ズームの出口が無い）

- **原因**: ダブルタップ抑止は `touchend` の350ms判定でやっているのに、`gesturestart` は
  無条件に `preventDefault` していた。**入口だけでなく、戻すためのピンチまでこちらが塞いでいた**。
  本作は稽古モードがタイトルロゴの3連打なので、ズームを踏む機会がむしろ多い
- **直し方**: 等倍のときだけ抑止する（`visualViewport.scale <= 1.01`）
- **確かめ方**: `visualViewport` を差し替えて `gesturestart` を投げ、`defaultPrevented` を見る

| scale | 1.0 | 1.01 | 1.5 | 3 | visualViewport 無し | 0 | NaN |
|---|---|---|---|---|---|---|---|
| 抑止する | ○ | ○ | **×（通す）** | **×（通す）** | ○ | ○ | ○ |

### わいわいの枠の中では記録が保持されない（`waiwai.save` へ移行）

仕様は SPEC §7-a。実装の型は御霊おとし `docs/DEVELOPMENT.md` §11-B をほぼそのまま移した。
**本作は御霊図鑑を持たないので、束は4値**（`best` / `title` / `titleRank` / `sound`）。

移すにあたって本作固有だった点が2つ。

- **`best` は1段ごとに `localStorage` へ書いていた。**御霊おとしは夜の終わりに1回だけ書く型だが、
  そのまま移すと**夜の途中でアプリを畳まれたときに最高段が消える**（従来は残っていた）。
  親フレームへの postMessage を毎段投げるのも筋が悪いので、`saveDirty` の旗を立てておき、
  **`visibilitychange`→hidden と `pagehide` で未書き込みがあれば書く**ことにした。
  実機で「終了させられる直前」に確実に来る最後の機会がそこ
- **音の好みは、タイトルの2択（音あり／静かに入る）が必ず上書きする。**束には載せるが、
  遊びの見え方は変わらない。遅れて届いた記録が選択を覆さないよう `soundChosen` の旗を置いた
  （御霊おとしと同じ）。**この移植で振る舞いを変えないことが目的**

#### 確かめた（`#autocut` は数段で夜明けになるので短時間で「残る」を測れる）

| 経路 | `waiwai.mode` | 保存先 | 残った | もう一夜 |
|---|---|---|---|---|
| ①ローカル配信（Pages相当・SDKあり） | standalone | `waiwai:fudakasane_save` | 8 → 8 ○ | ○ |
| ②`sdk.js` を落とした状態 | （SDKなし） | 旧キー4つ | 8 → 8 ○ | ○ |
| ③`dist/artifact.html` 素のまま `file://` | standalone | `waiwai:fudakasane_save` | 8 → 8 ○ | ○ |
| ④`dist/artifact.html` に Artifact 相当のCSP | （SDKなし） | 旧キー4つ | 8 → 8 ○ | ○ |

4経路とも `pageerror` 0。②④では `console.warn` が残る（**黙って落ちない**）。
④の `console.error` はCSPの拒否2件だけ（フォントと `sdk.js`。どちらもArtifactでは想定内）。

- **移行**: 旧キー4つだけを置いて開く → 束が
  `{"best":57,"title":"月導の架け手","titleRank":56,"sound":"off"}` になり、**旧キー4つが消え**、
  HUD最高 57・誉れ「月導の架け手」
- **大きいほうを採る**: 手元 12／わいわい側 72 → 72 が残り、称号も titleRank の大きい側

#### タウンの中（bridged）は stub でしか確かめていない

SDK が親として認めるのは `https://waiwai.town` / `localhost:8787` / `127.0.0.1:8787` の3つだけ
（`ALLOWED_PARENT_ORIGINS`）。**127.0.0.1:8787 に親ページの stub を立て**、ゲームを iframe に入れて
`hello-ack` / `save` / `load` を親の localStorage で中継した。
**これは筋が通ることの確認であって、本番の確認ではない。**

- `waiwai.mode` が `bridged` になり、記録は**親側**に入り、**iframe 自身の localStorage は空のまま**
  （＝守りたかった性質そのもの）。親ごと開き直すと HUD最高 8・誉れが戻る
- 親の返事を 600ms 遅らせても同じ（カードが出た時点で値は入っている）
- **親の返事が 1800ms（握手＋読みで計3.6秒）／`load` に返事をしない**とき: 2500msで切って warn 2行、
  **親側は空のまま**（＝読めていないので書かない、が効いている）。その夜は旧キーに書いて記録を落とさない

---

## 9. 【2026-08-30】御霊おとしの更新を移す（第2便）— 全国ランキングと番付

仕様は SPEC §7-b。実装の型は御霊おとし `docs/DEVELOPMENT.md` §10・§12 をそのまま移した。
本作固有だったのは**スコアの設計**と**送ってはいけない経路**の2つ。

### 80段が上限なので、段だけでは番付にならない

御霊おとしは得点に上限が無く同点問題が起きなかった。本作は **80段でクリア＝到達者が全員同点**になる。
企画の裁定（`kitan-circle/data/articles/20260830-1700-kasane-ranking-score-ruling.json`）は
**合成値 `段 * 100 + 浄化の通算`**。

- **後付けの別軸ではない。**ずれれば札が細り、ぴったり重ねれば幅が戻る——浄化は最初から勝敗を左右している
- **桁は構造で守られている**（浄化 ≦ 段 ≦ 80 < 100）ので、段が1つ違えば浄化では逆転しない
- **`getTopScores` は meta を返さない**（御霊おとし第12便で本番実測）。番付に浄化を出すには、
  score 単体から復元できる合成値であることが要る
- `purifyTotal` を新設した。既存の `streak` は連続数で、ずれると0に戻るので通算には使えない

### 自動プレイのハッシュが本番の番付を汚す（本作固有の落とし穴）

**`#autotest` / `#autocut` / `#autowobble` は稽古モードではなく、本番ルールで動く。**
記録を守る既存のガードは `!practice` だけなので、そのままでは
**検証を1回回すたびに理論最大値 8080 が本番の `main` ボードへ載る**。PV撮影の `#autowobble` も同じ。

- 送信条件を `canSubmitScore() = !practice && !autotest && !autocut && !autowobble` の1か所にまとめた
- `autotest` / `autowobble` の宣言はファイル末尾（ループの節）にあったので、状態の節へ前倒しした
- **`scripts/playtest.js` に4つ目の検証を足した**: stub の `waiwai` を置いて、3ルートすべてで
  `submitScore` の呼び出しが**0回**であることを毎回数える。ガードは書いたら終わりではなく、
  次に誰かが自動プレイの口を足したときに気づける形にしておく

### 結果カードが背の低い画面で溢れた（この変更で作り込んだ回帰）

- **症状**: 順位の2行で 536.4 → 587.4px（+51px）に伸び、表示領域 375×553（iPhone SE を
  Safari の上下バーごと想定）で**カードの上が 17.2px 画面の外**に出た
- **直し方**: 背の低い画面でだけ縦の余白と行間を詰める。札絵は到達の記念なので削らない・
  文字は12pxの床を割らない＝**縮めてよいのは余白だけ**
  ```css
  @media (max-height: 700px) {
    #overlay .card { padding-top: 18px; padding-bottom: 18px; gap: 8px; }
  }
  ```
- **`padding` の一括指定で書かない。**既存の `@media (max-width: 430px)` が `.card { padding: 26px 22px }` と
  **横を詰めてある**ので、一括で書くと横の余白まで押し戻す（`#overlay .card` のほうが詳細度で勝つので、
  書いた順に関係なく上書きされる）

| 表示領域 | カード高 | カード上端 | 「もう一夜」 | 収まる |
|---|---|---|---|---|
| 375×553（SEをバーごと想定） | 587.4 → **541.4** | -17.2 → **5.8** | 229×44 | ×→**○** |
| 375×667 | 587.4 → **541.4** | 62.8 | 229×44 | ○ |
| 390×844 | 587.4（据え置き） | 128.3 | 229×44 | ○ |
| 430×932 | 587.4（据え置き） | 172.3 | 229×44 | ○ |

番付への入口の釦は 180×24px（押し所の床24pxを満たす・`elementFromPoint` が `.rank-open` を返す）。
番付の札は 1人で 340×210px・10人＋自分で 340×579px。札の中の最小文字は 12px。

### 確かめた（タウンの応答は stub で模し、本番の `main` ボードは汚していない）

| 経路 | 順位の行 | 更新の印 | 番付への入口 | warn |
|---|---|---|---|---|
| タウン形の応答（rank 12・87人・上位10人） | 「全国 12 位 ／ 87人中」 | 出る | 出る（開く・✕で閉じる） | 0 |
| `getTopScores` が壊れた形（`entries` が文字列） | 「全国 12 位 ／ 87人中」 | 出る | **出ない** | 1 |
| `getTopScores` が返らない（2500msで切る） | 「全国 12 位」 | 出る | **出ない** | 2 |
| `submitScore` が返らない | **枠ごと出ない** | 出ない | 出ない | 2 |
| 上位が自分1人だけ | 「全国 12 位」 | 出る | 出る（札 340×210） | 1 |
| 素のSDK（Pages相当＝タウンの外） | **出ない**（載らないので正しい） | 出る | 出ない | 0 |
| `sdk.js` を落とす（Artifact の常態） | 枠ごと出ない | 出ない | 出ない | 2 |

7経路とも `pageerror` 0・「もう一夜」で再開できる。番付の行は
`1 ヒロ 80段（浄化41）` のように段と浄化を分けて出る。

### 未確認

- **わいわいの枠の中（bridged）での本番動作**。上記はすべて stub での確認で、タウン本番の親と
  突き合わせてはいない（自動操作のブラウザではクロスオリジン iframe の中身が描画されない）
- **本番の番付には本作のボードがまだ無い**（初回の送信で生まれる）。2人以上並んだ姿は作り値でしか見ていない
- 「最高記録を更新」は**合成値に対する印**なので、同じ段で浄化だけ増えた夜にも出る。
  HUDの「最高」は段のままなので数字は動かない。番付を開けば浄化の差が見える

---

## 10. 【2026-08-30】意匠を御霊おとしの系へ寄せる（金継ぎ・意匠担当）

御霊おとしが2026-08-28の金継ぎ健診で確定させた「意匠の系」（`mitama-otoshi/docs/SPEC.md` §9-b・
`kitan-works/docs/SHITSURAE.md`）へ、本作の `src/page.html` を寄せた。触ったのは `<style>` のみ。
診断書・前後の証跡は `kitan-circle/data/kintsugi/2026-08-30-kasane/`。

### 直した3件

1. **極小文字13件 → 0件**。12pxを床にした（`h1 small`・HUDラベル・カード内の各ラベル・footer等）。
   「SHIKIFUDA KASANE」は固有名詞のローマ字なので文字は残し、大きさだけ床まで上げた（SHITSURAE §3）。
   ただ大きくすると詰まるため、letter-spacing を段階的に詰めて幅を吸収した
2. **角丸6件 → 0件**。系（4/5/10px）の外だった `.stats`(8→10)・`canvas`(6→10)・`#final-fuda`(8→4)・
   `#retry`/`#start`/`#start-silent`(6→5) を揃えた。`#mute` は50%→5pxで `#bz-close` と同じアイコン釦の
   型に揃え、`#spirit-face` は御霊の肖像を丸い縁で見せる意匠と判断して50%のまま残した
3. **HUDラベルの日本語化**。`段 / BEST / 御霊` の `BEST` を `最高` に（御霊おとしの健診で確定した
   置き換え語と共通化）

新しい色・角丸・文字サイズは発明していない（`data/design-tokens.json` の範囲内で選んだ）。

### 依頼の3件以外に見つけたもの — 満月成就＋全国順位2行の組み合わせが375×553で破綻していた

§9で今朝収めた `@media (max-height:700px)` の詰めは「夜明け・順位2行」だけを見ており、
**満月成就（クリア）状態は栞の満願の言葉（`#final-quote`）と大きい札絵（172px）が追加で乗る**ため
対象外だった。文字の床上げ作業のためにこの状態を実際に作って測ったところ、着手前の実測で
**カード高さ660.3px**（375×553の表示領域に対し107px超過・中央寄せで上端-53.6px＝
**タイトルも「もう一夜」も両方とも画面の外に出て押せない**）ことが分かった。

CSSのみで安全弁を追加した（SHITSURAE §6「送りに倒す」）:

```css
.card {
  max-height: calc(100vh - 12px);
  overflow-y: auto;
  touch-action: pan-y; /* body の touch-action:none を打ち消さないと実機の指でスクロールできない */
}
```

天井を割ったときだけ中でスクロールする作りなので、通常の状態（天井の内側）では見た目は変わらない。
375×553で `scrollHeight 644 / clientHeight 539` を確認——全内容がスクロールで到達可能になった。
**スクロールできる手がかり（フェード等）は付けていない**。80段クリア＋全国順位2行が同時に出て
かつ画面が553px以下という極端な組み合わせでのみ起きるため、今回は見送り、次への申し送りとした。

### 前後の実測（375×553＝最も厳しい幅。他3幅は全状態で余裕あり）

| 状態 | 前 | 後 |
|---|---|---|
| 12px未満の文字 | 13件 | 0件 |
| 角丸が4/5/10/50%の外 | 6件 | 0件 |
| 結果カード（夜明け・順位2行あり） | 549.4px（ほぼ床） | 534.4px（`max-height:700px` の padding 18→14px・gap 8→6pxをさらに詰めて押し戻しを解消） |
| 結果カード（満月成就・順位2行あり） | 660.3px（両端とも画面外） | 541px（頭打ち・スクロールで全到達可） |
| タップ標的（`#retry`/`.rank-open`/`#bz-close`等） | 全て床(24px)以上 | 変化なし（角丸はタップ寸法に無関係） |

`node scripts/build-dist.js` でビルドし直し、`scripts/playtest.js` の4項目
（満月成就／夜明け／稽古は記録を残さない／番付を汚さない）は全て✅・exit 0を確認した。
色・コントラスト・JS・機能は変更していない。

---

## 11. 【2026-08-30】御霊おとしの更新を移す（第3便）— 帳を声の休符に合わせる／背の低い画面の詰め

### 帳が台詞の途中で開いていた

- **原因**: 開く時刻が 1100ms の決め打ちだった。声を聴いて決めた数字ではない
- **測った**: `voice_shiori_start.wav`（2.757秒）を20msのRMS包絡で無音を拾い、刻みを出した。
  読点「ページを、」のあとに **520ms の休符（1.300〜1.820秒）**がある。
  1100ms は「ページを、」を言っている最中で、**言葉の上に幕が動いていた**
- **直し方**: `DOOR_OPEN_AT = 1650`（= 350 + 1300 ＝休符の頭）。
  御霊おとしの帳（`350 + 1280`）と同じ考え方で、数字は本作の声から出した
- **ついでに直った**: 最初の御霊の名乗りが 3000ms 固定で、栞の台詞の終わり（3107ms）に**被っていた**。
  `DOOR_OPEN_AT + moveMs + 250 = 3400ms` へ送って重なりを解いた
- **確かめ方**: `#door` の class の変化を MutationObserver で拾い、ページを開いてからの ms を測る

| | 幕が動きはじめる | 幕を片付ける |
|---|---|---|
| 通常 | **1672ms**（休符の頭から22ms） | 3271ms（= 1672 + 1500 + 100） |
| `prefers-reduced-motion` | 1672ms | 2121ms（= 1672 + 350 + 100） |

  `pausedUntil` も `DOOR_OPEN_AT + moveMs` から計算するようにした（決め打ちの 2600ms をやめた）。

### 満月成就＋順位は、詰めても背の低い画面で天井を割る

意匠担当が §10 の床上げ（極小文字13件→0）の途中で見つけた。**いちばん背が伸びる組み合わせ**は
「満月成就（札絵＋栞の言葉）＋順位2行」で、375×553 では中身が 630px になる。

- 意匠側の手当て: `.card` に `max-height: calc(100vh - 12px)` ＋ `overflow-y: auto` ＋ `touch-action: pan-y`
  （天井の内側では見た目は変わらない）
- **それだけだと「もう一夜」が画面の外に居て、送れること自体が伝わらない。**実装側で
  `@media (max-height: 700px)` のときだけ `#overlay .card > #retry` を
  `position: sticky; bottom: 0` にした。**唯一必ず押せねばならない釦なので、送りの有無に
  かかわらず指が届く形にしておく。**地は金泥の不透明なので裏の文字は透けない

| 表示領域 | カード高 | 中身 | 送り | 「もう一夜」に指が届く |
|---|---|---|---|---|
| 375×553 | **541**（天井） | 630 | 要る | **○（貼り付けたので送らずに届く）** |
| 375×667 | 631.6 | 630 | 不要 | ○ |
| 390×844 | 704.6 | 703 | 不要 | ○ |
| 430×932 | 704.6 | 703 | 不要 | ○ |

`#autotest` で本物の満月成就まで通し、375×553 で `elementFromPoint` が `#retry` を返すこと・
**実際に押して再開できること**（overlay が消えて 0段から始まる）まで見た。`pageerror` 0。
`scripts/playtest.js` は4項目とも✅・exit 0。

---

## 12. 【2026-08-30】検品（結）の意地悪で出た4件を直す — `src/game.js`

移植4便を検品担当に回して出た指摘。**いちばん重いのは、記録が黙って潰れる筋が残っていたこと。**

### 「読めた」の判定が甘く、向こうの記録を今夜の低い段で潰していた（受入NG相当）

- **症状**: わいわい側が `{best: "42", titleRank: "42", title: "月導の架け手", sound: "on"}` のように
  **数を文字列型で返す**と、`mergeSave` の `typeof v === "number"` に弾かれて1つも取り込めない。
  それでも `saveUseSdk = true` のまま進み、夜の終わりに `save({best: 8, …})` を書く。
  **向こうにある42段の記録が、今夜の8段で消える**
- **原因**: SPEC §7-a の「読めなかった夜は書かない」を、`waiwaiTry` の `ok:false`
  （時間切れ・例外・拒否）だけで判定していた。**「応答は返ったが、形が違って何も取り出せなかった」**は
  `ok:true` を通り抜ける。`{ok, value}` で時間切れと「記録が無い」を分けたのに、その先で一緒にしていた
- **直し方**: `mergeSave` が**読み取れた「記録の」欄の数**を返すようにし、
  非nullの応答から1欄も取れなければ時間切れと同じ扱いにする（この夜は書かない・warn を残す）
- **一度目の直しは足りなかった**: 最初は `sound` も欄として数えたので、
  「段は型ちがいで読めなかったが音だけ読めた」夜が「読めた」と判定され、**まだ潰れた**。
  **`sound` は記録ではなく好み。**数えるのは `best` と `titleRank`+`title` だけにして直った
- **確かめ方**: 検品が書いた `qa-type-mismatch-followup.js` をそのまま流す。
  直す前は `saveCalls` に `{best: 8, …}` が載り、直したあとは **`saveCalls: []`**

### 壊れた巨大値が「80段＝満月成就」に化けていた

- **症状**: 旧キーに `best = "999999999999999999999"` があると HUD の最高が **80** になり、
  誉れに `titleRank` 由来の称号まで出る。**この遊びの最上位の誉れが、壊れたデータから捏造される**
- **原因**: `Math.min(MOON_FLOOR, …)` で丸めていた。**丸めると壊れた値が正当な値の顔をする**
- **直し方**: 0〜`MOON_FLOOR` の外は**丸めずに捨てる**（段の外の値は、正しい遊びからは出ない）
- **確かめ方**: 巨大値・負値とも HUD 0・誉れ無し。小数 `12.7` は 12 として取り込む（従来どおり）

### 番付に「999段」が並びうる

- **症状**: `getTopScores` が `score: 99999` を返すと、番付の行が「999段（浄化99）」になる
- **直し方**: 合成スコアの理論最大 `MAX_SCORE = 8080`（80段 × 100 + 浄化80）を超える項目は載せない
- **確かめ方**: `score` が 8080超／負／小数の項目が消え、残りの行はそのまま出る

### `total < rank` のとき、番付の見出しだけ辻褄が合っていなかった

- **症状**: 結果カードの行は「◯人中」を伏せるのに、番付の見出しには「全国 5人」が出る。
  その中に自分の「12位」が並ぶ
- **直し方**: 番付の見出しもカードの行と同じ条件（`total >= myRank`）で出す

### 検品が測って**出なかった**もの

`localStorage` が例外を投げる状態（warn を残して遊べる）／旧キーの非数値・`sound` の不正値／
`load()` が例外・無応答（カードは2766msで出る）／束のJSON破損／**稽古のあとに本番で遊ぶ**
（記録が混ざらない）／**夜の途中でタブを畳む→戻す**（5段で書かれ、戻して6段目へ続く）／
番付の名前に長い文字列・改行・HTMLらしき文字列（`textContent` なので素通し・20字で切れる）／
`rank` が飛ぶ・自分が1位・自分が圏外・`entries` が11件以上・未ソート（10件に切って rank 順に並ぶ）／
名前が空文字（「ナナシ」になる）。**いずれも `pageerror` 0。**

### 音の失敗を握りつぶしていた（同・検品）

- **症状**: `decodeAudioData` の失敗が `.catch(() => {})` で消える。琴は合成音へ落ちるので
  **「鳴っているのに実は読めていない」**が静かに起きる。台詞は無音になるが、ログには何も出ない
- **御霊おとしは2026-08-26のCSPの罠のあと warn を残す形に直っていた**。こちらだけ古い形のままだった
- **直し方**: `console.warn("[audio] <名前> を用意できなかった", e)`
- **確かめ方**: 琴2種＋台詞12本の**14音源すべて**を、ゲームと同じ経路（data URI は `atob`・
  それ以外は `fetch`）で取って `decodeAudioData` に通す。**両ビルドとも 14/14 成功・0失敗**
  （Pages相当＝外部assets／Artifact相当＝data URI・`connect-src 'none'` を被せた写し）

### 音とボイスの通し（実際に鳴る経路）

`AudioBufferSourceNode.prototype.start` を覗いて、80段の通し（`#autotest-sound`）で数えた。

| 見たもの | 結果 |
|---|---|
| 台詞が鳴った回数 | **12本**（開幕1150ms・帳のあと4200ms・段替わり9回・満願75415ms） |
| 琴・拍子木ほか | 462本 |
| ミュートで鳴っている最中の台詞 | **止まる**（1182ms開始 → 1809ms停止） |

期待どおりの内訳（栞の開幕1＋帳のあとの餡音1＋8段ごとの段替わり9＋満願1 = 12）。

### 画面（4寸法 × 4画面）

375×553 / 375×667 / 390×844 / 430×932 の各寸法で、タイトル・盤面・結果カード・**番付を開いた状態**を
数えた。**横はみ出し0・12px未満0件・押し所24px未満0件・`pageerror` 0**。番付の札は4寸法とも画面内。

検査器が挙げた `EM`(19×19) と `rank-open-mark`(5×17) は `.rank-open` 釦の**中の子要素**で、
`cursor: pointer` を継いだための誤検出。親の釦は180×24で押し所は足りている。
**押し所は「その要素が実際に押される単位か」まで見ないと、機械の数字に振り回される。**

---

## 13. 【2026-08-31】告知動画の音がゲームの実物でなかった — `scripts/pv/`

**オーナーが耳で気づいた。**「ボロン、ボロンみたいな短音っぽくなってる。実際のゲームの琴の音に
なっていないけど、これは意図的？」

### 症状と原因

- **実測**: 動画の各浄化の基音が**7回とも 196.5 Hz**（＝`koto_pluck_main.m4a` の素の基音）
- **原因**: `build-update-video.sh` が、ゲームの音を録らずに **ffmpeg で組み直していた**。
  同じサンプルを7回置き、**音量だけ**を 0.75→1.1 と上げていた。
  ピッチを動かす処理（`asetrate`・`atempo`）が一つも無い
- **ゲームの実物**（`sfxPerfect(streak)`）は陰旋法で**駆け上がる**。
  `kotoPluck(noteFreq(step))` が `rate = freq / KOTO_MAIN_HZ` でサンプルを変速し、
  **×3から二声**（`noteFreq(step+2)` を0.06秒遅らせて重ねる）、**×5で装飾句**（`kotoKira`）

| 浄化 | ×1 | ×2 | ×3 | ×4 | ×5 | ×6 | ×7 |
|---|---|---|---|---|---|---|---|
| 主音 | 233.1 | 293.7 | 329.6 | 349.2 | 440.0 | 466.2 | 587.3 Hz |
| 副音 | — | — | 440.0 | 466.2 | 587.3 | 659.3 | 698.5 Hz |

**「重ねるほど昇っていく」というこの遊びの気持ちよさが、音から丸ごと抜けていた。**
告知の本文が「浄化」を芯にしているので、ここが平坦なのは効く。

### 直し方 — 組み直すのをやめ、実物を録る

`capture-purify.js` は **CDP の `Page.startScreencast` で実時間**で撮っている
（フレームの `ev.metadata.timestamp` と `Date.now()` が同じ時計）。だから
**同じ1回の走行で音も録れば、画と音がそのまま揃う。**

- `AudioNode.prototype.connect` をフックし、`audioCtx.destination` へ繋がる接続
  （`sfxBus` / `voiceBus`）を `MediaStreamAudioDestinationNode` にも分岐させる
- **BGMは `<audio>` 要素**（`new Audio(BGM_DATA)`・DOMに現れない）なので、
  `window.Audio` を差し替えて生成を捕まえ、`createMediaElementSource` で同じグラフへ合流
- `MediaRecorder` で録る。**`--mute-audio` は出力デバイスを黙らせるだけ**で、WebAudioの処理も
  MediaStreamへの録音も止まらない
- **`--autoplay-policy=no-user-gesture-required` が要る**（`begin()` が内部の `setTimeout` から
  呼ばれる＝実ジェスチャーではないため）
- **ゲーム本体（`src/game.js`）は無改変。**撮影側だけで完結する

新規: `scripts/pv/capture-purify-sound.js` / `build-update-video-real.sh`。
旧 `build-update-video.sh` は冒頭に非推奨の注記を付けて経緯として残置。

### 確かめ方 — 音階表と突き合わせる

仕上がりの音を22050Hz・モノ・ハン窓のFFTにかけ、20msのRMS包絡で拾った発音点ごとに
上位ピークを出し、**音階表のどの音に一番近いか**で照合する（発音点の番号を先に決めつけない。
決めつけると1段ずれて「合っていない」と読み違える——実際に一度読み違えた）。

| 発音点 | 実測の主音 | 当たり |
|---|---|---|
| 2.12s | 234.8 Hz | ×1主（233.1・誤差0.7%） |
| 3.52s | 293.4 Hz | ×2主（293.7・0.1%） |
| 6.36s | 349.9 Hz | ×4主（349.2・0.2%） |
| 7.80s | 440.8 Hz | ×5主（440.0・0.2%）＋ 591.5＝×5副（587.3・0.7%） |
| 9.18s | 467.7 Hz | ×6主（466.2・0.3%）＋ 660.8＝×6副（659.3・0.2%） |
| 10.54s | 588.1 Hz | ×7主（587.3・0.1%）＋ 697.8＝×7副（698.5・0.1%） |
| 11.88s | 660.1 Hz | ×8主（659.3・0.1%） |

**基音が 234.8 → 293.4 → 349.9 → 440.8 → 467.7 → 588.1 → 660.1 Hz と昇り**、
×5以降は副音も期待値どおり立っている。栞の開幕の声と拍子木も入った（前は無かった）。

### 学び

**素材が本物でも、鳴らし方が違えば別の音になる。**
「実サンプル（`koto_pluck_*.m4a`）を重ねた」と「ゲームの音になっている」は別のこと。
音を使う成果物は、**実物を録るか、鳴り方の仕様を読んで数値で一致を示すか**のどちらかにする。

## 2026-09-01 下端の安全余白（ホームバー対策・シリーズ横並びの手当て）

「もう一夜」は結果札の下端に sticky で貼り付く作り（§8）なので、札そのものをホームバーの
帯の上に乗せる形にした。viewport-fit=cover は入っていたので、env() の余白側だけ。

- `#overlay { padding-bottom: env(safe-area-inset-bottom, 0px) }`
- `.card { max-height: calc(100vh - 12px - env(...)) }`（送りに倒す天井からも安全余白を引く。
  端末に帯が無ければ env=0 で従来と同じ）
- 検証: playtest 3ルート＋送信0回すべて緑。実機での「もう一夜が押せるか」はオーナーの iPhone 待ち
